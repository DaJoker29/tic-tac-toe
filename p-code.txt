START (Get X or O from user)

//Instantiate Game class

class game
    $turn = 0
    $move = X or O
    $playToken = arg(1)
    $aiToken = opposite(arg(1))
    $gameboard[9] = 0
    $final = 0
    $position

//Methods

    opposite($move)
        if $move == X
            return O
        endif
    return X
        
    record($gameBoard, $move, $position)
        $gameBoard[$position] = $move
    return

    redraw($gameboard, $move)
        clear interface //Need to figure out how
        redraw interface //Need to figure out how
    return

    check($gameBoard)
        if (any row is all aiTokens)
            return 2
        else if (any row is all playTokens)
            return 1
        else if (turn == 9)
            return 3
        endif

        $turn++
    return 0

    end($final)
        Print win, lose or draw
        reset()
    return
    
    reset()
        clear all variables and reset all counters
    return

END
